node_modules
dist
.DS_Store
.env

# Rendered output (not source assets)
out

# Generated voiceover audio — regenerate with `npm run generate-voiceover`.
# The folders are kept (see .gitkeep); the MP3s are not committed.
public/voiceover/**/*.mp3

# Next.js web app
web/.next
web/node_modules
web/.env.local
